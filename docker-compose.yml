services:
#  inventory-mysql:
#    container_name: inventory-mysql
#    hostname: inventory-mysql
#    image: mysql:8
#    restart: unless-stopped
#    environment:
#      MYSQL_ROOT_PASSWORD: root
#      MYSQL_DATABASE: inventory_db
#    ports:
#      - "3306:3306"
#    volumes:
#      - inventory-mysql_data:/var/lib/mysql

  inventory-service:
    build: .
    ports:
      - "8085:8085"
      #    environment:
      #      SPRING_DATASOURCE_URL: jdbc:mysql://inventory-mysql:3306/inventory_db?allowPublicKeyRetrieval=true&useSSL=false
      #      SPRING_DATASOURCE_USERNAME: root
      #      SPRING_DATASOURCE_PASSWORD: root
      #    depends_on:
      #- inventory-mysql
    #      - kafka_inventory
    networks:
      - kafka_default
      -
volumes:
  inventory-mysql_data:
  kafka_data:

networks:
  kafka_default:
    driver:
      bridge